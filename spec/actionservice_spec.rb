require 'spec_helper'

describe Actionservice do
  it 'has a version number' do
    expect(Actionservice::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
